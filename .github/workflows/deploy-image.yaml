name: Deploy Docker Image

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: Linux
    container:
     image: betrand1997/httpd


    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build Docker image
        run: docker build -t app .
        #### wwww
